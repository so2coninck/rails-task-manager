<h1>All the tasks</h1>


<ul>
    <%@tasks.each do |task| %>
   <li><%=link_to task.name, task_path(task) %> <%=task.id%>: <%= task.name%> (<%=task.description %>)</li>
 <% end %>
</ul>

<%= link_to "add a new task", new_task_path %>

